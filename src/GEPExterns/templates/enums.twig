{% for name, values in class.enums %}
/**
 * @typedef {(string|number)}
 */
{{ name }};
{% for value in values %}
/**
 * @type {name}
 */
{{ class.name }}.prototype.{{ value }};
{% endfor %}
{% endfor %}