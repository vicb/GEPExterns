/**
 * @constructor
{% if parents[class.name] %}
 * @extends {{ '{' ~ parents[class.name] ~ '}' }}
{% endif %}
*/
{{ class.name }} = function() {};
{% include 'methods.twig' %}
{% include 'properties.twig' %}
{% include 'enums.twig' %}